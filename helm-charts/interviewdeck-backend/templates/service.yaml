apiVersion: v1
kind: Service
metadata:
  name: {{ include "interviewdeck-backend.fullname" . }}
  labels:
    {{- include "interviewdeck-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "interviewdeck-backend.selectorLabels" . | nindent 4 }}
